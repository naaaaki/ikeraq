# Ikeraq

GitHub で注目されているリポジトリを、日本語で紹介するサイト。

**読む順番：** `docs/DECISIONS.md`（決定事項・仕様書より優先）→ `docs/SPEC.md`（v0.6・履歴）

---

## 守ること

| # | ルール | 理由 |
|---|---|---|
| 1 | **日次パイプラインで LLM を呼ばない** | ランニングコストをドメイン代のみに保つ（SPEC §9.1） |
| 2 | **`Promise.all` で全件を並列リクエストしない** | `mapLimited` を使う（SPEC §10.2） |
| 3 | **閾値を変えたら `docs/criteria.md` も直す** | 判定基準の公開が信頼の源泉 |
| 4 | **警告は「疑い」までしか言わない** | 断定は名誉毀損リスクを生む |
| 5 | **README のコードを転載しない** | 抜粋は最大500字（SPEC §8.2） |
| 6 | **追跡対象を減らさない** | 追跡をやめた期間のスター履歴は永久に欠損する |

Naoki は Claude Code を使って記事を書く。**パイプラインから LLM を呼ぶこととは別**（D-002）。

---

## 記事を書くときの流れ

1本20分が目安。週3本（D-005）。

### 0. 先に取り込む（★毎回・省略不可）

```bash
npm run sync
```

`git pull --rebase --autostash` を実行する。**書き始める前に必ず。**

省略してはいけない理由が、このリポジトリには固有にある。
**毎朝6時（JST）に bot が `data/` 全体を書き換えてコミットしている**（`daily.yml`）。
つまり朝以降に作業を始めた時点で、リモートはすでに1コミット先行している。

さらに、記事を書くと `npm run evaluate` が `data/repos/*.json` を書き換える。
これは **bot が触るファイルとまったく同じ**。2026-09-08 には、
こちらが触った6ファイルすべてを bot も同じ日に触っていた
（行が違ったので自動マージできたが、`stars` のように同じ行を触れば競合する）。

取り込みを最後に回すと、次の2つが起きる。

| 症状 | 原因 |
|---|---|
| `Please commit your changes or stash them` で pull が止まる | 未コミットの変更を抱えたまま pull した |
| 毎回マージコミットが増える | 分岐してから合流しているため |

`--autostash` を付けてあるので、書きかけがあっても止まらずに取り込める。

### 1. 候補を見る

```bash
npm run note
```

### 2. 下書きを作る

```bash
npm run note -- owner/name
```

`data/notes/{owner}/{name}.md` ができる。書き方は `docs/article-template.md`。

### 3. 調べて、書く

**必須は「どういうものか」だけ。** ほかは書けたら書く。

- 英語の description を訳しただけにしない
- 触っていないこと・確かめていないことは書かない
- 「使い方」は**実際に動かせたときだけ**。動かせなければ節ごと消す（D-006）

**★ README を読む前に、既定ブランチを確認する。**
`main` を決め打ちで読むと、古い README を根拠に書いてしまう。
2026-09-13 に Graphify-Labs/graphify でこれが起き、記事の中心主張が本家に無い状態になった
（既定ブランチは `v8`。`main` には別作者時代の旧版が残っていた）。

**★「未解決の issue は N件」と書かない。**
GitHub が返す `open_issues_count` は**プルリクエストを含む**。
issue だけの数を出すなら search API で数え直す。まとめて書くなら
「issue とプルリクエスト合わせて N件」と、含むものを明記する。

**★ 無料版と有料版がある場合は、線引きを先に確かめる。**
無料側にない機能を無料側の説明に混ぜやすい。差が料金だけなのか、機能もなのかを分けて書く。

**★ `data/repos/*.json` の数字をそのまま本文に書かない。**
リリース回数と貢献者数は API を節約するため**7日に1回しか更新していない**（`collect.ts` の `staleCounts`）。
スター数も取得日時点の値。記事に書くなら「60回以上」「30人台」のように丸める。
issue の件数は上の項目のとおり。

**★ 数字は条件ごと写す。** 「N件のタスク」と「延べN回の実行」は別。
比較の数字は、比較対象（ベースライン）が示されているかまで見る。示されていなければ
「N%落ちた」とは書けない。

### 4. 図を作る（★原則必須）

**すべての記事に図を入れる（D-010）。** 頼まれなくても作る。
入れないと判断した場合は、**その理由を Naoki に必ず伝える。** 黙って省かない。

認められる理由は3つだけ。

| 理由 | 補足 |
|---|---|
| 説明が箱ひとつで終わる | 絵にすると情報が減る |
| 見た目そのものが主役 | UIツール・テーマ。スクリーンショットのほうが正確 |
| 形が思いつかない | ★**思いつかない理由まで書く**（「流れが無く並列の列挙しかない」など） |

迷ったら「**A が B を通って C になる**」と言えるか試す。言えるなら図にできる。
「A と B と C がある」だけなら箇条書きで足りる。

図は次の手順で作る。

<!-- Claude Code へ：この節は毎回そのとおりに実行すること -->

1. **書き上がった「どういうものか」を読む。** 説明の中心にある動きを1つ選ぶ
2. **「何が → どうなって → 何になる」を1文にする**（日本語で。キャプションの下地にもなる）
3. **SVG を直接書く。** AI画像生成は使わない。理由は D-008
4. `viewBox="0 0 800 450"`（16:9）。色は3色だけ
   （背景 `#FFFFFF` / 墨 `#17160F` / 緑 `#1E5A48`）
5. 組み立ては次のとおり。**型は決め打ちしない**ので、内容に合わせて崩してよい
   - 上に**見出し1行**（いちばん大きく。キーワードだけ緑にする）
   - 中に**横並びの要素**。名前は太く、役割は小さくグレーで（　）に入れる
   - 矢印は緑で太め（`stroke-width="4"` 程度）＋三角の矢尻
   - 下に**締めの一行**。絵が言っていないことを補う
6. **文字は `font-family: var(--jp)` を指定する。** ページの書体を継がせるため
7. `role="img"` と `aria-label` を必ず付ける。**中身は書き下ろさず、図に書いた文字を写す**
   - SVG の `<text>` を上から順にたどって拾う。キャプションの言い換えにしない
   - **いちばん大きい見出しを冒頭に、締めの一行を末尾に、そのまま入れる**
   - 図に無い文を「添えられている」と書かない。図より強い言い方にしない
   - 書けたら `<text>` と1つずつ突き合わせ、拾い漏れと混入がないか確かめる

   ★ 2026-09-13 に公開済み20本を確認したところ、**20本すべてでここがずれていた**。
   内訳は「見出しが入っていない」（20本）「図に無い文を説明している」（過半）。
   画面で見る人と読み上げで聞く人に違う内容が届くので、毎回この4点を確認する。

**★ 外部ファイルにしない。** HTML に直接書く。`<img>` にするとフォントが効かず、
図だけ書体が変わって貼り付け感が出る。

出したあとに、**キャプションと `image_alt` の下書きも一緒に渡す。**

- キャプション … 絵から読み取ってほしい**意味**。絵の説明ではない
- `image_alt` … 絵に**何が描かれているか**。目の見えない人向け

**図を入れないほうがいい場合**（そのときは作らないと言う）

- 説明が箱ひとつで終わる（絵にすると情報がゼロになる）
- 見た目が主役（UIツール・テーマ）→ スクリーンショットのほうがよい
- 形が思いつかない

### 5. 抜けを確認する

```bash
npm run note -- --check
```

### 6. 事実確認を通す（★毎回・省略不可）

**`fact-checker` エージェントに渡す。** Naoki に見せる前に必ず挟む。

書いているのは**スターが急に伸びた新しいリポジトリ**の紹介で、書き手の知識が届いていない
対象が多い。READMEを読んで書いていても、もともと知っていたつもりの内容が混ざる。
その混入を、公開情報（README・リポジトリ情報）と1文ずつ突き合わせて洗い出す。

- 図（SVGの中の文字）も対象。図にだけ間違いが残る事故を防ぐ
- 指摘は `×食い違い` `△出典なし` `▲言い過ぎ` の3種。**×と▲は直す。△は消すか和らげる**
- 直すのは書き手の仕事。fact-checker は指摘までしかしない
- 複数本まとめて渡してよい（ただし1体あたり4〜5本まで。それ以上は途中で止まる）

**「READMEを読んで書いたから大丈夫」は通らない。** 読んだうえで混ざるのが、この作業の事故の形。

### ★ 直したあと、もう一度通す（2巡。D-016）

**1巡で終わらせない。** 2026-09-14 に公開中52本を直したとき、
**修正の過程で新しい誤りを12件持ち込んでいた**（すべて2巡目で検出）。

| 型 | 例 |
|---|---|
| 反対側へ振り切る | 「OCRは付いてこない」を直したら「OCRは入っています」と無条件の断定に |
| 片方だけ直す | 本文を直して図が古いまま／注意点が古いまま |
| 修正で文を壊す | 「ただし、まったく動かないわけではありません」（意味が通らない） |

**直すときの手順：**

1. 直す前に、一次情報で「正しい向き」を確定させる。**反対語への置き換えで直さない**
2. 直したら、同じ語・同じ数字で**記事全体を検索する**（本文・図・読み上げ文・注意点）
3. 直した段落を読み直す（文が壊れていないか）
4. **渡された指摘一覧を信じない。** 必ず一次情報に当たる（一覧そのものの誤りが4件あった）
5. 直し終えたら、もう一度 fact-checker に通す

### 7. 公開する

**必ず Naoki 自身が読み直してから。** 生成したまま載せない（D-002 N3）。
事実確認（6）を通す前に Naoki に見せない。

---

## よく使うコマンド

```bash
npm run sync              作業前に取り込む（★毎回）
npm run note              候補を見る
npm run note -- a/b       下書きを作る
npm run note -- --check   下書きの抜けを確認
npm run collect           日次収集（要 GITHUB_TOKEN）
npm run evaluate          機械判定（外部APIを呼ばない）
npm run stats             実測レポート
npm test                  テスト
npm run typecheck         型チェック
```
