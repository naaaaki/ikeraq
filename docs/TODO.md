# 課題表

最終更新：2026-09-01

**優先度の考え方**

| | 意味 |
|---|---|
| **S** | 1日遅れるごとに、取り返せない損が出る |
| **A** | これが無いと公開できない |
| **B** | 公開はできるが、あったほうが確実に良い |
| **C** | 余力があれば |

**担当**

- **Naoki** … Naoki にしかできない（アカウント作成・法務・お金・最終判断）
- **開発** … Claude Code で進められる

---

## S｜いますぐ

| # | 課題 | 担当 | 内容 |
|---|---|---|---|
| ~~S-1~~ | ~~GitHub リポジトリを作り、Secrets を登録する~~ | — | **済**（2026-08-30）。`naaaaki/wakuru` 作成・`WAKURU_GITHUB_PAT` 登録（現 `IKERAQ_GITHUB_PAT`）・初回収集151件を記録 |
| ~~S-2~~ | ~~Discord の Webhook URL を登録する~~ | — | **見送り**（2026-08-30・Naoki判断）。`monitor.ts` は重大な異常のとき `exit 1` で Actions を失敗させるため、**「静かに止まった」は GitHub の標準メール通知で拾える**。エラーが頻発するようなら再検討する |
| ~~S-3~~ | ~~GitHub の失敗メールが届く状態か確認する~~ | — | **済**（2026-09-01）。`Settings → Notifications → Actions` が「失敗したワークフローのみ通知」になっていることを確認。Discord を見送った以上、これが唯一の警報 |

スター履歴の蓄積は 2026-08-30 から始まった。以後は毎朝6時に自動で積まれる。

---

## A｜公開までに必ず

| # | 課題 | 担当 | 内容 |
|---|---|---|---|
| ~~A-1~~ | ~~連絡先を書く~~ | — | **済**（2026-08-30）。`dino.spike.web3@gmail.com` を `/about/` と `/privacy/` に掲載 |
| ~~A-2~~ | ~~プライバシーポリシー~~ | — | **済**（2026-08-30）。レビュー済み。`src/lib/site.ts` に連絡先を集約 |
| ~~A-3~~ | ~~ドメインを決める~~ | — | **済**（2026-09-01）。`ikeraq.com` を Cloudflare Registrar で取得。サイト名も Wakuru → Ikeraq に改名。**商標（J-PlatPat）の確認は未実施** |
| ~~A-4~~ | ~~Cloudflare Pages へデプロイ~~ | — | **済**（2026-08-30）。https://wakuru.pages.dev/ で公開。`WAKURU_SITE_URL` と `NODE_VERSION=22` を設定済み。全ページ200・存在しないURLは404・canonical は pages.dev を指す |
| ~~A-4d~~ | ~~GitHub の Secret に `IKERAQ_GITHUB_PAT` を追加~~ | — | **済**（2026-09-01）。旧 `WAKURU_GITHUB_PAT` は日次収集の成功を1回見届けてから削除する |
| ~~A-4e~~ | ~~Cloudflare Pages の環境変数を差し替える~~ | — | **済**（2026-09-01）。`IKERAQ_SITE_URL` = `https://ikeraq.com`。canonical・og:url が新ドメインを指すことを実機で確認 |
| ~~A-4f~~ | ~~Pages にカスタムドメイン `ikeraq.com` を追加~~ | — | **済**（2026-09-01）。`ikeraq.com` `www.ikeraq.com` とも 200。**www が apex へ 301 していない**ので、Cloudflare のリダイレクトルールで寄せるべき（A-8） |
| ~~A-4g~~ | ~~GitHubリポジトリ名を `ikeraq` に変更~~ | — | **済**（2026-09-01）。`naaaaki/ikeraq`・`git remote set-url` 済み・fetch 確認済み。**改名後も Cloudflare Pages の連携は維持された**（2230e2a のデプロイが走ったことを確認）。ローカルフォルダも `projects/ikeraq` に改名済み・`package-lock.json` の name も追随。**Cloudflare Pages のプロジェクト名 `wakuru` は変更不可**（作り直すしかないため据え置き。訪問者が見るURLは `ikeraq.com` なので実害なし） |
| ~~A-4b~~ | ~~デプロイ後に Cookie を確認する~~ | — | **済**（2026-08-31）。実機のブラウザで確認：JSから見える Cookie 0件・localStorage 空。サーバー応答にも `Set-Cookie` なし。HttpOnly の Cookie はツールから直接読めないが、文面が「当サイト自身は設定していない／配信事業者が用いる場合がある」と主語を分けてあるため、仮に Cloudflare が付けても矛盾しない。**プライバシーポリシーの修正は不要**と判断 |
| ~~A-4c~~ | ~~`public/robots.txt` を削除する~~ | — | **済**（2026-09-01）。削除ではなく**全ページ許可＋Sitemap 明示に書き換えた**。消すだけでも解除にはなるが、サイトマップの在り処を伝えられないため |
| ~~A-5~~ | ~~Search Console 登録~~ | — | **済**（2026-09-01）。**ドメインプロパティ**で `ikeraq.com` を登録（Cloudflare のため所有権確認は自動）。サイトマップは `https://ikeraq.com/sitemap-index.xml` を送信。★ドメインプロパティは先頭が固定されないため、**フルURLでないと無効になる** |
| ~~A-6~~ | ~~実データでの通し確認~~ | — | **不要と判断**（2026-08-30）。151件・189ページを実測したところ**ビルド1.47秒**（1ページ約8ms）。上限1000件でも10秒程度で、Cloudflare の20分制限には遠く届かない。件数増加は懸念材料から外す |
| ~~A-8~~ | ~~`www.ikeraq.com` を `ikeraq.com` へ 301 で寄せる~~ | — | **済**（2026-09-01）。Cloudflare の「ルール → リダイレクトルール」で設定。**一括リダイレクトではない**（あれは URL の対応表用）。動的・301・クエリ保持で構成。実機確認：トップ・`/about/`・クエリ付きURL のいずれも 301 でパスとクエリを維持したまま apex へ着地し、追跡すると 1 回のリダイレクトで 200。apex 自体は 200 でループなし |
| **A-7** | **運営者名義を専門家に確認** | Naoki | 個人情報の取り扱いを説明する文書には、扱う主体の記載が要る。いまはハンドルネーム（`naaaaki`）で置いている。本名・屋号のどこまでが必要かは判断が要る。あわせて、第三者のアカウント名掲載の位置づけと、Cloudflare 利用時の越境移転の記載要否も |

---

## B｜あると効く

| # | 課題 | 担当 | 内容 |
|---|---|---|---|
| B-1 | 記事を書きためる | Naoki＋開発 | 週3本（D-005）。**一覧に日本語が出るのは記事があるものだけ**。ここが増えるほどサイトの価値が上がる |
| B-2 | 今週のまとめ（`/weekly/`） | 開発 | 記事が10本ほどたまってから。SEOの中核（structure-proposal） |
| B-3 | 一覧のページ送り | 開発 | いま24件で切っている。件数が増えてから |
| B-4 | 日本語の検索方法を決める | 開発 | Postgres標準の全文検索は日本語を単語に切れない。件数が300を超えたあたりで |
| **B-5** | **OGP画像** | 開発 | SNS流入用。SPEC Phase 3。**優先度を上げるべき**（2026-09-01 レビュー）。いま X に貼ると `twitter:card=summary` の画像なしカードになる。立ち上げ直後の現実的な流入はSNSなので、Phase 3 まで待つ理由が弱い |
| B-6 | 判定の閾値を実測で調整 | 開発 | 7〜14日ぶん貯めてから `npm run stats` で確認（SPEC §15） |
| B-7 | 暗い配色（ダークモード） | 開発 | モックが明るい配色だけなので保留中 |
| B-8 | ロゴ・配色の確定 | Naoki | いまは暫定 |
| **B-9** | **リポジトリページのタイトル文言を決める** | Naoki | 2026-09-01 のレビューで、英語のみだったタイトルを日本語化した。**いまの文言は仮**。紹介文ありは「見出しの一文」をそのまま使い、紹介文なしは `owner/name のスター数とライセンス` にしている。変えるなら `src/pages/repo/[owner]/[name].astro` の `pageTitle` の1か所 |
| **B-10** | **紹介文つきだけを見る導線** | Naoki＋開発 | 601件のうち日本語の紹介文があるのは11件。一覧を眺めると「ほとんど英語のまま」に見える。「日本語README」と同じ形で絞り込みページを作るか判断したい。作れば11件でも体験が変わるが、件数の少なさが目立つ面もあるので方針の判断が要る |
| **B-11** | **スマホ表示の実機確認** | 開発 | 2026-09-01 のレビューでは、ブラウザのウィンドウが最大化されていてリサイズできず未確認。CSS のブレークポイントは 900px と 560px の2つで、サイドバーの回り込み・言語列の非表示は入っている。実機で崩れが無いかだけ見る |

---

## C｜余力があれば

| # | 課題 | 担当 | 内容 |
|---|---|---|---|
| C-1 | ニュースレター | Naoki | 収益の本命（SPEC §3）。受け口が決まるまでリンクを出していない |
| C-2 | アフィリエイト設置 | Naoki | 表記の仕組みは実装済み。**始めるときは `src/lib/site.ts` の `HAS_AFFILIATE` を `true` にする**と、最上部の帯・about・プライバシーポリシーの3か所が同時に切り替わる。あわせてプライバシーポリシーに事業者名を追記すること |
| C-3 | サイト内検索 | 開発 | 300件を超えてから |
| C-4 | 特集 | Naoki＋開発 | D-007。記事がたまってから決める |
| C-5 | 対象レベル表示（初心者/中級/上級） | 開発 | 機械では判定できないので、記事を書くとき人が付ける |
| C-6 | 言語別・トピック別RSS | 開発 | SPEC Phase 4 |

---

## 技術的に抱えている借金

| # | 内容 | 対応方針 |
|---|---|---|
| T-1 | **Astro 5 を使っている**（最新は7系） | D-009 で判断済み。7系はこのPCで動かない（スマートアプリコントロール）。**脆弱性報告は残るが、該当機能を1つも使っていない**。使いたくなったら D-009 を読み直す |
| T-2 | 記事の図は Markdown を通さない | SVG が壊れるため（2026-08-29 に修正）。図を書くときは HTML で書くこと |
| ~~T-3~~ | ~~データが1件しかない~~ | **解消**（2026-08-30）。151件を追跡中 |
| T-4 | 分類の「そのほか」が**159件（26%）** | 12分類に当てはまらないものが残る。無理に埋めていない。実データが増えてからルールを育てる（SPEC §6.3）。**2026-09-01 時点で159件まで増え、比率も悪化している**（記録時は34件・23%）。一覧では最後に固定したので目立たなくなったが、分類規則そのものは要見直し |

---

## 済んだもの（2026-08-30）

- **GitHub リポジトリ作成・トークン設定・初回プッシュ**
- **初回のデータ収集151件**。スター履歴の蓄積が始まった
- **記事を1本追加**（deepseek-ai/deepseek-harness・図つき）
- 一覧で紹介文つきを先頭に固め、境目に区切りを入れた
- パンくずから「そのほか」を排除。分類の精度を 41% → 23% に改善
- マスコットを4か所に配置（画像は98%軽量化）
- **プライバシーポリシーを作成**（レビュー済み）、連絡先を掲載
- `docs/article-cost.md`（記事1本あたりの消費量の実測）

## 済んだもの（2026-08-29）

- サイト本体10ページ（トップ・個別・日次・カテゴリ・言語別・日本語README・殿堂入り・サイト説明・判定基準・RSS・404）
- 一覧を日本語主体に（記事の「見出しの一文」を主役に、英語は原文として補助）
- カード全体をクリック可能に
- 記事の図（SVG）が壊れていた不具合の修正
- GitHub リンクを枠つきボタンに刷新
- sitemap（noindex のページは除外）、構造化データ（Article + BreadcrumbList）
- ステマ表記・免責（全ページ固定）
